#!/usr/bin/env node

var paycore = require('paycore-node/lib/cli/paycore');
var path = require('path');
var servicesPath = path.resolve(__dirname, '../');
var additionalServices = ['insight-api', 'insight-ui'];
paycore(servicesPath, additionalServices);
